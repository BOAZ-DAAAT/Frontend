server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # SPA 라우팅: /connect, /playground 를 새로고침해도 404가 안 나게
    # 해당 파일이 없으면 index.html을 돌려줘서 React Router가 처리하게 한다
    location / {
        try_files $uri $uri/ /index.html;
    }
}